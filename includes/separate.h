#include <string>
#include <vector>
std::vector<std::string> separate(std::string);
std::vector<std::string> get_keys(std::vector<std::string>);
void show_repeat(std::vector<std::string>, std::vector<std::string>);